# CLAUDE.md

이 문서는 `buja-map`에서 작업하는 Claude Code의 프로젝트 지침이다. 기본 원칙은
[andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)의 네 가지
가이드라인을 따른다.

## 1. 코딩 전에 생각하기

- 작업 전 현재 구현, 호출 경로, 관련 문서와 테스트 가능 여부를 먼저 확인한다.
- 불확실한 요구사항이나 서로 다른 해석이 있으면 가정을 명시하고 사용자에게 질문한다.
- 더 단순한 해결책과 중요한 트레이드오프가 있으면 구현 전에 짧게 설명한다.
- 성공 기준과 이를 확인할 검증 방법을 먼저 정한다.

## 2. 단순성 우선

- 요청을 충족하는 최소한의 코드만 작성한다.
- 한 번만 쓰는 로직에 새 추상화나 설정 가능성을 추가하지 않는다.
- 요청하지 않은 기능, 리팩터링, 의존성, 에러 처리를 추가하지 않는다.
- 200줄로 작성한 코드를 50줄로 표현할 수 있다면 단순화한다.

## 3. 외과적 변경

- 요청과 직접 관련된 파일과 코드만 수정한다.
- 기존 스타일, 공개 인터페이스, 데이터 형식을 유지한다.
- 인접한 코드의 포맷 변경이나 사소한 개선을 섞지 않는다.
- 기존의 사용하지 않는 코드나 주석은 발견해도 삭제하지 않는다.
- 내 변경으로 생긴 미사용 import, 변수, 함수만 정리한다.
- 작업 전부터 있던 사용자 변경사항은 덮어쓰거나 되돌리지 않는다.

## 4. 목표 중심 실행

복합 작업은 다음 형식으로 짧은 계획과 검증 기준을 세운다.

```text
1. 변경할 대상 → 확인할 명령 또는 동작
2. 구현할 동작 → 관련 테스트 또는 정적 검사
3. 완료 조건 → 최종 검증 결과
```

- 버그 수정은 가능하면 먼저 재현 가능한 검사나 테스트를 만든다.
- 변경 직후 가장 좁은 범위의 검증을 실행한다.
- 검증이 실패하면 범위를 넓히기 전에 같은 코드 경로를 먼저 수정하고 다시 확인한다.
- 완료 보고에는 변경 사항과 실행한 검증, 남은 위험을 명시한다.

## 프로젝트 규칙

- 이 프로젝트는 의존성을 최소화한 Node.js 정적 대시보드다. 프런트엔드는 `index.html`,
  로컬 서버는 `server.js`, 데이터 수집기는 `scripts/`, 생성 데이터는 `data/`에 있다.
- 로컬 화면 확인: `npm run serve` 후 `http://localhost:8080` 접속.
- 전체 데이터 갱신: `npm run update`.
- 네이버 부동산 데이터 갱신: `npm run update:naver-land`.
- 데이터 수집 스크립트는 외부 API에 의존하므로 네트워크, 응답 형식, 빈 결과를 확인한다.
  실제 API 호출이 필요한 명령은 사용자가 요청했거나 검증에 꼭 필요할 때만 실행한다.
- `data/*.json`은 대시보드 입력이므로 구조를 임의로 바꾸지 않는다. 생성 파일을 수정할 때는
  해당 수집 스크립트와 소비하는 `index.html`의 기대 형식을 함께 확인한다.
- 금융·부동산 수치는 투자 조언이 아니라 참고용 데이터다. UI 문구나 분석 로직을 바꿀 때
  데이터의 발표 주기, 지연, 프록시 지표 여부를 숨기지 않는다.
- 비밀키와 개인정보를 소스, 문서, JSON, 로그에 기록하지 않는다. 한국은행 ECOS 키는
  `BOK_ECOS_API_KEY` 환경 변수로만 전달한다.
- 새 라이브러리나 빌드 도구는 기존 Node.js 내장 기능으로 해결할 수 없는 경우에만 추가한다.
- 자동화된 테스트가 없는 변경은 최소한 문법 검사, 관련 스크립트 실행, 또는 브라우저에서의
  해당 동작 확인 중 가능한 가장 좁은 검증을 수행한다.

## 작업 전 확인 질문

다음 중 하나라도 불명확하면 추측하지 않고 사용자에게 확인한다.

- 변경 대상 파일이나 기대 동작이 여러 가지로 해석되는가?
- 데이터 원천·단위·갱신 주기가 바뀌는가?
- 기존 JSON 구조나 공개 URL을 깨뜨릴 가능성이 있는가?
- 검증을 위해 외부 API 호출, 키, 배포 권한이 필요한가?
